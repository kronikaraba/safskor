# Lokal dev'de boş bırak (Vite proxy /api -> localhost:4000).
# Prod'da Vercel'e tanımla: Railway backend URL'i, sonunda / olmadan.
# Örn: VITE_API_URL=https://safskor-server.up.railway.app
VITE_API_URL=
